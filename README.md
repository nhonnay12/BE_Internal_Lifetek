1. Install
- clone Project  
  `git clone https://github.com/nhonnay12/BE_Internal_Lifetek.git`

2. Install dependency
   `npm install`
3. Create .env File
   `cp .env.example .env`
4. Run project
   `npm start`
